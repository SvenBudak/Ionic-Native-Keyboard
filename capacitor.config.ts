import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.native.keyboard',
  appName: 'ionic-native-keyboard',
  webDir: 'dist'
};

export default config;
